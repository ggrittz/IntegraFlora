# UC total records

This folder contains all the original records for each conservation unit.
You can populate it and update it by running [analyses/getOccs.R](analyses/getOccs.R).
This will overwrite these files.